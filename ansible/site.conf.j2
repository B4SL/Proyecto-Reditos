server {
    listen {{ nginx_port }};
    server_name {{ domain_name }};
    root {{ app_root }};

    location / {
        try_files $uri $uri/ /index.html;
    }

    location ~ /\.ht {
        deny all;
    }
}